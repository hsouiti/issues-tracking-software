import {LoginForm} from '@features/auth';

export const Login = () => {
  return (
    <>
      <div style={{margin: '50px 0', textAlign: 'center'}}>login Page</div>
      <LoginForm />
    </>
  );
};
